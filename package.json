{
  "name": "helicone-ollama-proxy",
  "version": "1.0.0",
  "description": "proxy for helicone and ollama",
  "main": "dist/proxy.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node -r dotenv/config dist/proxy.js",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jules",
  "license": "ISC",
  "dependencies": {
    "@helicone/helpers": "^1.0.11",
    "@types/node": "^22.14.0",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}
